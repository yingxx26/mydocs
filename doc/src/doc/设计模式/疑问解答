1，为什么装饰器模式和代理模式要 实现 同一个接口？
  因为只是加功能，不改变接口
2，基本采用继承/实现 + 聚合 的模式（组合很少用）

3，适配器/装饰器/代理模式 区别
适配器   是为了 转换匹配,需要改接口（每次接口不同无法递归），（继承或实现最后目标者，持有被适配者的引用）
装饰器   是为了增加功能（对原来对象无感知），顺序可以随意改变，不需要改接口（支持递归组合） ， （继承或实现被装饰者，持有被装饰者的引用）
代理模式 采用组合的方式，编译期已经确定  不改接口（继承或实现被代理者，持有被代理者的引用）
