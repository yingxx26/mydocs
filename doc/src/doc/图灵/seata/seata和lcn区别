seata和lcn比较，有什么不一致？

此题可能会被面试官问到，需要多多注意哦

1，seata和lcn大致的实现思路是一致的，但是回滚的机制不一样。
2，lcn是采取代理数据源的模式，再根据发起方执行本地事务的结果进行回滚或者提交
3，seata采取的是根据undo_log日志表，进行逆向生成sql语句，来解决回滚
4，lcn能够保证强一致性，但可能发生死锁的现象
5，seata能保证最终一致性，但可能造成脏读
