
用户
商品
订单 

问题：cpu100




新闻  搜索房源 房源详情 地图找房 网上预约  网上签约 统计分析   

房源详情 和 网上预约       比较简单 ，访问量大 ，两三台，缓存

网上签约                         选中房子 ， 填写用户信息，调用三方接口查档，婚姻，填写价格、银行，根据模板生成合同，

统计分析                         统计新增用户，用户最喜欢的楼盘，用户最近一次预约时间，最近几天预约次数，  一开始直接sparksql，后来用elasticsearch


领域建模

herbernate 改源码
模板模式


mybatis  委派 装饰器 工厂  代理
如何循环
动态生成组件

<component :is="item.component" v-for="item in items" style="margin-button: 10px;" @rainData="rainData"></component>


get post区别

GET在浏览器回退时是无害的，而POST会再次提交请求。
GET产生的URL地址可以被Bookmark，而POST不可以。
GET请求会被浏览器主动cache，而POST不会，除非手动设置。
GET请求只能进行url编码，而POST支持多种编码方式。
GET请求参数会被完整保留在浏览器历史记录里，而POST中的参数不会被保留。
GET请求在URL中传送的参数是有长度限制的，而POST么有。
对参数的数据类型，GET只接受ASCII字符，而POST没有限制。
GET比POST更不安全，因为参数直接暴露在URL上，所以不能用来传递敏感信息。
GET参数通过URL传递，POST放在Request body中。


多态作用
对外提供一致接口和方便扩展

段式管理程序，页式管理文件


Sql去重
日期类线程安全  Swagger

给比人提供接口 jar
Springcloud  的网关

Jwt 三个组成部分 

责任链模式

适配器是为了 shipei
装饰器 是为了增加功能

迭代器模式

数据库死锁  二级缓存问题  锁和事务范围问题

mybatis  装饰器 ，代理