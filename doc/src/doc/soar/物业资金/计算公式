循环变动明细集合 计算：
定期： 取变动后余额最小的那个的值 乘以利率
活期： 1，上次结息日的变动后余额-定期（上面的值）=活期初始金额
       2，大于0的变动（缴存），直接存为未动用
       3，小于0的变动（使用）， 分解，有两种情况
             3,1： 够用 ，分成两条数据 1：动用 2：未动用
             3,2： 不够用，递归
       4，根据时间不同，计算动用和未动用的利息，相加

真正结息：存储过程JX_3_ZHJXMX_FROM_GS_WXJ
